#include <stdio.h>
#include <stdlib.h>

int main(void) {

	// Cria um array de 50 inteiros (200 bytes)
	int *v = malloc(200);
	// Cria um array de 200 chars (200 bytes)
	char *x = malloc(200);
	return 0;
}
